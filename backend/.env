PORT=8787
# Use the Firestore emulator (recommended for dev). The Firebase CLI starts it on 8080 by default.
FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
AUTH_DISABLED=1
DEV_UID=dev-local
DEV_EMAIL=dev@local
GOOGLE_CLOUD_PROJECT=ptcg-premium-dev
TZ=America/Sao_Paulo
